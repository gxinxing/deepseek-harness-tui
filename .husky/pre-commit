#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# ── Self-repair: auto-format + auto-lint-fix staged JS files ──────────────
# lint-staged reads the "lint-staged" key from package.json
pnpm lint-staged

# ── Self-inspect: full check (lint + format + test) before commit ──────────
# Uncomment to enforce full check on every commit (slower but thorough):
# pnpm check
